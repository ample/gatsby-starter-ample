import { Meta } from "@storybook/addon-docs/blocks"

<Meta title="Utilities/Intro" />

# Utilities

Utilities are the React components, but they serve as shared utilities for other components, rather than being an object a content editor can directly manipulate.
