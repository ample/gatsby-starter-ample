import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks"

import Link from "./"
import { ExternalLink, InternalLink } from "./"

<Meta title="Components/Link" component={Link} />

# Link

The `<Link />` component determines whether the route passed to it is an internal or external link, and wraps the component appropriately.

If an internal link, the component renders [Gatsby's `<Link />` component](https://www.gatsbyjs.org/docs/gatsby-link/), otherwise it renders an anchor tag.

## Usage

<Preview>
  <Story name="Internal">
    <Link to="/components/link">I'm an internal link</Link>
  </Story>
</Preview>

<Preview>
  <Story name="External">
    <Link to="https://ample.co">
      <em>And I'm external</em>
    </Link>
  </Story>
</Preview>

## Properties

While the `<Link />` component will accept and pass on all accepted properties, the properties are use conditionally depending on the type of link (i.e. _internal_ v _external_). Those are broken out below:

### External Link

<Props of={ExternalLink} />

### Internal Link

<Props of={InternalLink} />
