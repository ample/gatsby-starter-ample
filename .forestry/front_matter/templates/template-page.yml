---
label: "[Template] Page"
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: The URL for the page is created from the title. The title also serves
    as the default meta title if the SEO section is left blank. Otherwise, the page
    title can be used for organization in the CMS.
- name: sections
  type: field_group_list
  fields:
  - name: container
    type: include
    config: {}
    template: section-container
    label: Container
  config:
    min: 
    max: 
    labelField: title
  label: Sections
  description: Sections are configurable groups of components that make up each page.
    You can add as many sections and rearrange as needed.
- name: seo
  type: field_group
  config: {}
  fields:
  - name: seo
    type: include
    config: {}
    template: admin-seo
    label: SEO
  label: SEO
  description: While there are sensible defaults, the SEO section provides the option
    to explicitly set meta tag values.
pages:
- src/content/pages/content-page.md
- src/content/pages/index.md
