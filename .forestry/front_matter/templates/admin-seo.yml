---
label: Admin - SEO
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
  hidden: true
- name: title_template
  type: text
  config:
    required: true
  description: The title template will inject the current page where "%s" while the
    rest of the content is static.
  label: Title Template
- name: default_image_src
  type: file
  config:
    maxSize: 250
  label: Default Image
  description: When there is no obvious fallback for a page's meta image, we'll use
    this one instead.
pages:
- src/content/admin/seo.md
