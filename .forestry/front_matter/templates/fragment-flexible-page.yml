---
label: "[Fragment] Flexible Page"
hide_body: true
fields:
- name: blocks
  type: blocks
  label: Blocks
  template_types:
  - component-column
  - component-container
  config:
    min:
    max:
  description: Blocks can be configured to add content to your page in a flexible way.
